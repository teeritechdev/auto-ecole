package com.autoecole.repository;

import com.autoecole.entity.ConfigurationApplication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConfigurationApplicationRepository extends JpaRepository<ConfigurationApplication, Long> {
}
