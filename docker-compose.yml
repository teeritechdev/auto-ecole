version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: autoecole-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: auto_ecole_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
    driver: local
