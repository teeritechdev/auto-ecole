package com.autoecole.repository;

import com.autoecole.entity.CodeConfiguration;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CodeConfigurationRepository extends JpaRepository<CodeConfiguration, Long> {
}
